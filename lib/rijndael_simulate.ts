export const SBOX: number[] = [
  ,
];

export const INV_SBOX: number[] = [
  
];

const RCON = [0x00, 0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36];

function gmul(a: number, b: number): number {
  let p = 0;
  for (let c = 0; c < 8; c++) {
    if ((b & 1) !== 0) p ^= a;
    const hi_bit_set = (a & 0x80) !== 0;
    a = (a << 1) & 0xff;
    if (hi_bit_set) a ^= 0x1b;
    b >>= 1;
  }
  return p;
}

function subWord(w: number[]) {
  return [SBOX[w[0]], SBOX[w[1]], SBOX[w[2]], SBOX[w[3]]];
}

function rotWord(w: number[]) {
  return [w[1], w[2], w[3], w[0]];
}

export function expandKeyAes256(key32: number[]): number[][][] {
  let w: number[][] = [];
  for (let i = 0; i < 8; i++) {
    w.push([key32[4*i], key32[4*i+1], key32[4*i+2], key32[4*i+3]]);
  }
  for (let i = 8; i < 60; i++) {
    let temp = [...w[i-1]];
    if (i % 8 === 0) {
      temp = subWord(rotWord(temp));
      temp[0] ^= RCON[i/8];
    } else if (i % 8 === 4) {
      temp = subWord(temp);
    }
    w.push([
      w[i-8][0] ^ temp[0],
      w[i-8][1] ^ temp[1],
      w[i-8][2] ^ temp[2],
      w[i-8][3] ^ temp[3],
    ]);
  }
  
  // Group into 15 round keys (each is a 4x4 matrix, column-major)
  const roundKeys: number[][][] = [];
  for (let i = 0; i < 15; i++) {
    const rk = Array.from({ length: 4 }, () => Array(4).fill(0));
    for (let c = 0; c < 4; c++) {
      for (let r = 0; r < 4; r++) {
        rk[r][c] = w[i*4 + c][r];
      }
    }
    roundKeys.push(rk);
  }
  return roundKeys;
}

export interface RoundTrace {
  round: number;
  startState: string[][];
  afterSubBytes?: string[][];
  afterShiftRows?: string[][];
  afterMixColumns?: string[][];
  afterAddRoundKey: string[][];
  roundKey: string[][];
}

const toHexMatrix = (m: number[][]) =>
  m.map(row => row.map(b => b.toString(16).padStart(2, '0').toUpperCase()));

export function simulateAllRounds(block16: number[], key32: number[]): RoundTrace[] {
  const roundKeys = expandKeyAes256(key32);
  const traces: RoundTrace[] = [];
  
  let state = Array.from({ length: 4 }, () => Array(4).fill(0));
  for (let c = 0; c < 4; c++) {
    for (let r = 0; r < 4; r++) {
      state[r][c] = block16[c*4 + r] || 0;
    }
  }
  
  // Initial AddRoundKey (Round 0)
  const startState0 = toHexMatrix(state);
  for (let c = 0; c < 4; c++) {
    for (let r = 0; r < 4; r++) {
      state[r][c] ^= roundKeys[0][r][c];
    }
  }
  traces.push({
    round: 0,
    startState: startState0,
    afterAddRoundKey: toHexMatrix(state),
    roundKey: toHexMatrix(roundKeys[0])
  });
  
  // 14 Rounds
  for (let round = 1; round <= 14; round++) {
    const startState = toHexMatrix(state);
    
    // SubBytes
    state = state.map(row => row.map(b => SBOX[b]));
    const afterSubBytes = toHexMatrix(state);
    
    // ShiftRows
    state = [
      [state[0][0], state[0][1], state[0][2], state[0][3]],
      [state[1][1], state[1][2], state[1][3], state[1][0]],
      [state[2][2], state[2][3], state[2][0], state[2][1]],
      [state[3][3], state[3][0], state[3][1], state[3][2]],
    ];
    const afterShiftRows = toHexMatrix(state);
    
    // MixColumns
    let mixColumnsState = Array.from({ length: 4 }, () => Array(4).fill(0));
    if (round < 14) {
      for (let c = 0; c < 4; c++) {
        const s0 = state[0][c], s1 = state[1][c], s2 = state[2][c], s3 = state[3][c];
        mixColumnsState[0][c] = gmul(2, s0) ^ gmul(3, s1) ^ s2 ^ s3;
        mixColumnsState[1][c] = s0 ^ gmul(2, s1) ^ gmul(3, s2) ^ s3;
        mixColumnsState[2][c] = s0 ^ s1 ^ gmul(2, s2) ^ gmul(3, s3);
        mixColumnsState[3][c] = gmul(3, s0) ^ s1 ^ s2 ^ gmul(2, s3);
      }
      state = mixColumnsState;
    }
    const afterMixColumns = round < 14 ? toHexMatrix(state) : undefined;
    
    // AddRoundKey
    for (let c = 0; c < 4; c++) {
      for (let r = 0; r < 4; r++) {
        state[r][c] ^= roundKeys[round][r][c];
      }
    }
    const afterAddRoundKey = toHexMatrix(state);
    
    traces.push({
      round,
      startState,
      afterSubBytes,
      afterShiftRows,
      afterMixColumns,
      afterAddRoundKey,
      roundKey: toHexMatrix(roundKeys[round])
    });
  }
  
  return traces;
}

export function simulateAllInverseRounds(block16: number[], key32: number[]): RoundTrace[] {
  const roundKeys = expandKeyAes256(key32);
  const traces: RoundTrace[] = [];
  
  let state = Array.from({ length: 4 }, () => Array(4).fill(0));
  for (let c = 0; c < 4; c++) {
    for (let r = 0; r < 4; r++) {
      state[r][c] = block16[c*4 + r] || 0;
    }
  }
  
  // Initial AddRoundKey (Round 0) - using the last round key for decryption
  const startState0 = toHexMatrix(state);
  for (let c = 0; c < 4; c++) {
    for (let r = 0; r < 4; r++) {
      state[r][c] ^= roundKeys[14][r][c];
    }
  }
  traces.push({
    round: 0,
    startState: startState0,
    afterAddRoundKey: toHexMatrix(state),
    roundKey: toHexMatrix(roundKeys[14])
  });
  
  // 14 Rounds (descending round key index)
  for (let round = 1; round <= 14; round++) {
    const startState = toHexMatrix(state);
    
    // InvShiftRows
    state = [
      [state[0][0], state[0][1], state[0][2], state[0][3]],
      [state[1][3], state[1][0], state[1][1], state[1][2]],
      [state[2][2], state[2][3], state[2][0], state[2][1]],
      [state[3][1], state[3][2], state[3][3], state[3][0]],
    ];
    const afterShiftRows = toHexMatrix(state);

    // InvSubBytes
    state = state.map(row => row.map(b => INV_SBOX[b]));
    const afterSubBytes = toHexMatrix(state);
    
    // AddRoundKey
    for (let c = 0; c < 4; c++) {
      for (let r = 0; r < 4; r++) {
        state[r][c] ^= roundKeys[14 - round][r][c];
      }
    }
    const afterAddRoundKey = toHexMatrix(state);

    // InvMixColumns
    let mixColumnsState = Array.from({ length: 4 }, () => Array(4).fill(0));
    if (round < 14) {
      for (let c = 0; c < 4; c++) {
        const s0 = state[0][c], s1 = state[1][c], s2 = state[2][c], s3 = state[3][c];
        mixColumnsState[0][c] = gmul(0x0e, s0) ^ gmul(0x0b, s1) ^ gmul(0x0d, s2) ^ gmul(0x09, s3);
        mixColumnsState[1][c] = gmul(0x09, s0) ^ gmul(0x0e, s1) ^ gmul(0x0b, s2) ^ gmul(0x0d, s3);
        mixColumnsState[2][c] = gmul(0x0d, s0) ^ gmul(0x09, s1) ^ gmul(0x0e, s2) ^ gmul(0x0b, s3);
        mixColumnsState[3][c] = gmul(0x0b, s0) ^ gmul(0x0d, s1) ^ gmul(0x09, s2) ^ gmul(0x0e, s3);
      }
      state = mixColumnsState;
    }
    const afterMixColumns = round < 14 ? toHexMatrix(state) : undefined;
    
    traces.push({
      round,
      startState,
      afterSubBytes,
      afterShiftRows,
      afterMixColumns,
      afterAddRoundKey,
      roundKey: toHexMatrix(roundKeys[14 - round])
    });
  }
  
  return traces;
}
